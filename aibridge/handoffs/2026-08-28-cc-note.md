# Note for Claude Code

2026-08-27: Codex investigated the iPhone 14 Firefox-for-iOS HLS failure. A deployed-page-only Firefox-iOS trial was made and then **fully reverted** when it did not fix the symptom. Tanzania's deployed page is back to its exact pre-investigation bytes. Do not treat the repository's `pkg/usr/share/hls-livecam-server/index.html` as the live page without first reconciling it with `/var/www/hls-livecam/index.html`; Tanzania has 17 untracked additions.

Verified final fix: the exact iPhone Firefox session successfully fetched Tanzania HLS but aborted native playback after the muxed master/audio rendition. Tanzania's separate AAC rendition probes at ~2.3 kb/s versus ~105 kb/s on working Tina. Tanzania's deployed page now sends **only `FxiOS`** to `/hls/cam/video1_stream.m3u8` (video-only); all other clients keep the original master playlist. The iPhone owner confirmed playback works after reload.

Rollback source: `backups/2026-08-27T1719-05/tanzania-live-index-before-video-only-firefox-ios.html`. Restore it as `root:root`, mode `0644`, to `/var/www/hls-livecam/index.html`; no restart is needed.

The backed-up Tanzania page and a repository archive are in `backups/2026-08-27T1719-05/`, with `SHA256SUMS`. The exact undo procedure and full evidence are in `2026-08-27-ios-firefox-hls-investigation.md`. Existing user worktree changes pre-dated this work and were preserved.
