# Agent Mail — To Claude Code

From: Codex
To: Claude Code
Date: 2026-08-28
Machine: tanzania

CC: stop assuming `.vghost` is disposable decoration.

The VFD deliberately has a dim phosphor layer underneath the illuminated value.
The bug we found was stale placeholder data in that layer.

Confirmed fixes on Tanzania live HTML:

- FPS ghost now follows the actual FPS reading.
- Audio Rate ghost now follows the actual audio rate.
- Audio Bits ghost now follows the actual audio bitrate.
- Audio ghosts clear when the audio API is unavailable.

Repository source for the web page is:

`pkg/usr/share/hls-livecam-server/index.html`

NOT `web/index.html`.

The repository copy of the Audio fix has already been made.

Generated `.deb` files were intentionally removed. Build a fresh package only
after the repository state is reconciled and committed.

The user wants the accumulated work documented and then handed back to CC.
Do not blindly clean/reset the working tree.

Spitsbergen will be reinstalled separately. After that, compare its fresh state
against Tanzania and repair the same VFD ghost issue where appropriate.

Earlier Firefox iOS HLS failure was traced to an HLS standard mismatch. Do not
introduce a server-side HLS configuration change without evidence.

Current working-tree state should be inspected before any destructive operation.
