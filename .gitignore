# Python
.venv/
__pycache__/
*.pyc

# Runtime state (generated)
state/*.pid
state/logs/
state/enabled
state/mediamtx.yml
state/dark.png
state/dark_orig.png
state/broadcast.txt
state/buzz.txt
state/msg_lock
state/camdash-feed.state
state/cell_aspect

# Rendered from template by setup
web/index.html

# Large vendored binary (re-downloadable)
vendor/mediamtx

# Packaged DMGs -- rebuildable from scripts/build-macos-dmg.sh, ~600 MB each
# because they carry the whole venv. These must never reach the remote.
dist/

# Ad-hoc backups taken before risky refactors
.packaging-backup/
.pre-combined-av-backup-*/

# macOS
.DS_Store

# Local Claude Code state
.claude/
web/cams/cams.json
